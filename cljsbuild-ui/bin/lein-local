#!/bin/bash

# Get full path to the directory of this script.
#   source: http://stackoverflow.com/a/246128/142317
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Run our local lein script with our local lein jar,
#   and pass all arguments to it.
LEIN_JAR=$DIR/lein.jar $DIR/lein "$@"
